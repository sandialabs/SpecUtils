// Replacement for src/behavior/index.js — keeps d3.behavior.drag and d3.behavior.zoom.
// (event/drag.js is a different file, kept under event/, and remains a dependency of zoom.)

import "behavior";
import "drag";
import "zoom";
